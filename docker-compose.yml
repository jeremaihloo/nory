version: '2'
services:
  db:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=123456
  web:
    image: jeremaihloo/ncms
    command:
      - "python"
      - "ncms.py"
      - "--debug=False"
      - "--db_host=db"
    depends_on:
      - db
    links:
      - db:db